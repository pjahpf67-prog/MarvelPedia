package com.marvelpedia.ui.scores

import android.view.LayoutInflater
import android.view.ViewGroup
import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView
import com.marvelpedia.R
import com.marvelpedia.data.ScoreEntry

class ScoreHistoryAdapter(private val items: List<ScoreEntry>) :
    RecyclerView.Adapter<ScoreHistoryAdapter.ViewHolder>() {

    class ViewHolder(view: android.view.View) : RecyclerView.ViewHolder(view) {
        val difficulty: TextView = view.findViewById(R.id.history_difficulty)
        val score: TextView = view.findViewById(R.id.history_score)
    }

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder {
        val view = LayoutInflater.from(parent.context)
            .inflate(R.layout.item_score_history, parent, false)
        return ViewHolder(view)
    }

    override fun onBindViewHolder(holder: ViewHolder, position: Int) {
        val entry = items[position]
        holder.difficulty.text = entry.difficulty.name
        holder.score.text = "${entry.score} / ${entry.total}"
    }

    override fun getItemCount(): Int = items.size
}
