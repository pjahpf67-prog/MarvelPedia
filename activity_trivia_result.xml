package com.marvelpedia.ui.encyclopedia

import android.content.Intent
import android.os.Bundle
import android.text.Editable
import android.text.TextWatcher
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.fragment.app.Fragment
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView
import com.google.android.material.textfield.TextInputEditText
import com.marvelpedia.R
import com.marvelpedia.data.Character
import com.marvelpedia.data.CharacterRepository

class EncyclopediaFragment : Fragment() {

    private lateinit var adapter: CharacterAdapter

    override fun onCreateView(
        inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?
    ): View {
        return inflater.inflate(R.layout.fragment_encyclopedia, container, false)
    }

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        val recycler: RecyclerView = view.findViewById(R.id.character_recycler)
        val searchInput: TextInputEditText = view.findViewById(R.id.search_input)

        adapter = CharacterAdapter(CharacterRepository.characters) { character ->
            openDetail(character)
        }

        recycler.layoutManager = LinearLayoutManager(requireContext())
        recycler.adapter = adapter

        searchInput.addTextChangedListener(object : TextWatcher {
            override fun beforeTextChanged(s: CharSequence?, start: Int, count: Int, after: Int) {}
            override fun onTextChanged(s: CharSequence?, start: Int, before: Int, count: Int) {
                val query = s?.toString().orEmpty()
                val filtered = if (query.isBlank()) {
                    CharacterRepository.characters
                } else {
                    CharacterRepository.search(query)
                }
                adapter.updateList(filtered)
            }
            override fun afterTextChanged(s: Editable?) {}
        })
    }

    private fun openDetail(character: Character) {
        val intent = Intent(requireContext(), CharacterDetailActivity::class.java)
        intent.putExtra(CharacterDetailActivity.EXTRA_CHARACTER_ID, character.id)
        startActivity(intent)
    }
}
